# Git LFS — 원화/스프라이트 등 대용량 아트 리소스
# 주의: 아래 경로의 파일들은 이미 LFS 포인터로 커밋되어 있음.
# 이 파일이 없으면 체크아웃 시 포인터 텍스트가 그대로 남아
# Unity 텍스처 임포트가 실패하고 아이콘 참조가 깨진다.
Assets/_Project/Art/**/*.png filter=lfs diff=lfs merge=lfs -text
Assets/_Project/Resources/Hunt/*.png filter=lfs diff=lfs merge=lfs -text
